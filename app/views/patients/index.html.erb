<h1>Patients</h1>

<ul>
    <% @patients.each do |patient| %>
        <li>
            <%=link_to patient.name, patient_path(patient) %>
        </li>
    <%end%>
</ul>

<%=link_to "New Patient", "/patients/new", method: "post" %>



